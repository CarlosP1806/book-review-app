<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Best Books</title>

  <link rel="stylesheet" href="/styles.css">

  <!-- Import font awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>

<body>
  <%- include("partials/navbar.ejs") %>

  <main class="book-info-main">
    <article class="book-info">
      <figure class="book-img">
        <img src="<%= bookData.volumeInfo.imageLinks.thumbnail%>" alt="book cover">
      </figure>
      <div class="data-container">
        <h2><%= bookData.volumeInfo.title %></h2>
        <div class="author-container">
          <% for(let i=0; i < bookData.volumeInfo.authors.length; i++) { %>
            <span class="author">
              <%= bookData.volumeInfo.authors[i] %>
              <% if(i < bookData.volumeInfo.authors.length -1) { %>
                <%= ',' %>
              <% } %>
            </span>
          <% } %>
        </div>
        <p><%= bookData.volumeInfo.description %></p>
      </div>
    </article>
    
    <section class="review-section">
      <div class="section-title">
        <% if(reviews.length) { %>
          <h2 class="title">Reviews</h2>
        <% } else { %>
          <h2 class="title">No Reviews Yet</h2>
        <% } %>
      </div>
      <% if(reviews.length) { %>
        <% for(let i = 0; i < reviews.length; i++) { %>
          <%- include("partials/book_review.ejs", { reviewData: reviews[i]}) %>
        <% } %>
      <% } %>
    </section>
  </main>

  <script type="module" src="/navbar.js"></script>
</body>